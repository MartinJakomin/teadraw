import React from "react";

export function SpectatorBanner() {
  return (
    <div className="spectator-banner" role="status">
      Spectator mode — you can follow the game, but you cannot submit prompts, draw, vote, or advance rounds.
    </div>
  );
}
